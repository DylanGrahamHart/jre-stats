{
  "scripts": {
    "deploy": "./gradlew build; gsutil cp build/libs/*.jar gs://jre-stats-jars/derp.jar; gcloud compute instances stop instance-1; gcloud compute instances start instance-1",
    "startup": "gsutil cp startup.sh gs://jre-stats-jars/startup.sh"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^4.0.2",
    "gulp-sourcemaps": "^2.6.5"
  }
}
